name: Release

on:
  push:
    branches: ["main", "master"]
    tags: ["v*"]
  pull_request:
    branches: ["main", "master"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            binary_name: biolab
            asset_name: biolab_unix
            upload_name: biolab_unix
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary_name: biolab.exe
            asset_name: biolab_win.exe
            upload_name: biolab_win.zip
          - os: macos-latest
            target: x86_64-apple-darwin
            binary_name: biolab
            asset_name: biolab_mac_amd64
            upload_name: biolab_mac_amd64
          - os: macos-latest
            target: aarch64-apple-darwin
            binary_name: biolab
            asset_name: biolab_mac_arm64
            upload_name: biolab_mac_arm64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry
        uses: Swatinem/rust-cache@v2

      - name: Install musl-tools (Linux)
        if: contains(matrix.target, 'musl')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Run tests
        run: cargo test

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: |
          mv target/${{ matrix.target }}/release/${{ matrix.binary_name }} ${{ matrix.asset_name }}

      - name: Package Windows binary
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path ${{ matrix.asset_name }} -DestinationPath ${{ matrix.upload_name }} -Force
          $hash = (Get-FileHash ${{ matrix.upload_name }} -Algorithm SHA256).Hash.ToLower()
          "$hash  ${{ matrix.upload_name }}" | Out-File -Encoding ascii "${{ matrix.upload_name }}.sha256"

      - name: Generate checksum
        if: runner.os != 'Windows'
        run: shasum -a 256 ${{ matrix.asset_name }} > ${{ matrix.asset_name }}.sha256

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.upload_name }}
          path: |
            ${{ matrix.upload_name }}
            ${{ matrix.upload_name }}.sha256

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*/*
          generate_release_notes: true
