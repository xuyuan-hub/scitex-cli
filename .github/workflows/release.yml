name: Release

on:
  push:
    branches: ["main", "master"]
    tags: ["v*"]
  pull_request:
    branches: ["main", "master"]

env:
  CARGO_TERM_COLOR: always

jobs:
  verify-release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Verify tag matches Cargo package version
        shell: bash
        run: |
          cargo_version="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | select(.name == "scitex-cli") | .version')"
          test "${GITHUB_REF_NAME}" = "v${cargo_version}"

  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            binary_name: scitex
            asset_name: scitex_unix
            upload_name: scitex_unix
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary_name: scitex.exe
            asset_name: scitex_win.exe
            upload_name: scitex_win.zip
          - os: macos-latest
            target: x86_64-apple-darwin
            binary_name: scitex
            asset_name: scitex_mac_amd64
            upload_name: scitex_mac_amd64
          - os: macos-latest
            target: aarch64-apple-darwin
            binary_name: scitex
            asset_name: scitex_mac_arm64
            upload_name: scitex_mac_arm64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry
        uses: Swatinem/rust-cache@v2

      - name: Install musl-tools (Linux)
        if: contains(matrix.target, 'musl')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run tests
        run: cargo test

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: |
          mv target/${{ matrix.target }}/release/${{ matrix.binary_name }} ${{ matrix.asset_name }}

      - name: Package Windows binary
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path ${{ matrix.asset_name }} -DestinationPath ${{ matrix.upload_name }} -Force
          $hash = (Get-FileHash ${{ matrix.upload_name }} -Algorithm SHA256).Hash.ToLower()
          "$hash  ${{ matrix.upload_name }}" | Out-File -Encoding ascii "${{ matrix.upload_name }}.sha256"

      - name: Generate checksum
        if: runner.os != 'Windows'
        run: shasum -a 256 ${{ matrix.asset_name }} > ${{ matrix.asset_name }}.sha256

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.upload_name }}
          path: |
            ${{ matrix.upload_name }}
            ${{ matrix.upload_name }}.sha256

  release:
    needs: [build, verify-release]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*/*
          generate_release_notes: true
